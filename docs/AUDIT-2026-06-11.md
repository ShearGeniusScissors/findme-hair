# findme.hair AUDIT — 2026-06-11 (takeover session)

Live host: https://www.findme.hair · Repo: ~/Projects/findme-hair · Deploy: npx vercel --prod

## DB truth (read live 2026-06-11, Supabase taxuueemqtquihhzhgnr)
- Active listings: **13,872** (homepage shows 13,830 — slightly stale but dynamic; fine)
- Listings with google_photos: **0 of 13,872** ← every card site-wide renders an empty placeholder
- ai_descriptions containing raw `**` markdown: **4,729** (~34% of profile pages show literal asterisks)
- Claimed listings: **0** ← "Claimed only" homepage filter returns zero results (trap)
- With Google rating: 13,478 · avg **4.72** · total Google reviews across listings: **1,340,248** ← huge REAL trust number, currently unused

## P0 BUGS (fix regardless of playbook)
1. **Raw markdown in About copy** — `app/salon/[slug]/page.tsx:502` renders `business.ai_description` raw; `stripMarkdown` (lib/seoMeta.ts) is only applied to meta. Visible on ~4,729 pages ("**Alee Hair** brings…").
2. **Mobile horizontal overflow on profile pages** — opening-hours rows and Details address/phone clip off the right edge at 390px.
3. **Empty 16:9 placeholder on every BusinessCard** (components/BusinessCard.tsx:54) — since 0 listings have photos, ~50% of every card is dead beige. Mobile suburb pages show ~7 cards per 3200px. Fix: collapse/replace image area when no photo (keep code path for when photos arrive). DO NOT mass-populate Google photos tonight — Places photo media costs would be ongoing per-view (finance watch-item: runaway Google Cloud spend).
4. **"Claimed only" filter chip on home** — 0 claimed listings → guaranteed empty results. Hide until claims exist.
5. **Claim page off-brand + dead** (/claim) — hot-pink unbranded CTA + red back-link (not the locked palette; clearly unstyled defaults), ~70% of the page is empty space below the form, zero social proof/value restatement. Restyle with EXISTING brand tokens only (no new hex — colour freeze respected).

## STRUCTURE / DEAD SPACE
- **Geo suburb page (/vic/[region]/[suburb])**: 2-col card grid + map column; map ends after ~1 viewport leaving the right rail empty for the rest of the page → make map sticky, and cards denser (placeholder collapse does most of this).
- **Profile page**: text-only hero; grey empty image box in sidebar; About = single wall-of-text paragraph (brutal on mobile); Reviews section thin (rating + count only); sticky mobile call bar EXISTS (good); claim sidebar card exists (good); SG "Preferred scissors" link exists (good); "Last visit: X" real signal in footer (good).
- **Pivot suburb pages (/hairdresser/[suburb] etc.)**: already have guide content + FAQ JSON-LD + scissor plug. Decent.
- **Home**: strong. Live count in search button; stats strip (13,830 / 8 states / Hair only / Free); claim band. Minor rhythm only.
- **/for-salons**: solid; 7th benefit card orphans the grid row.
- No single master spacing variable (unlike SG): spacing is per-component Tailwind utilities (py-10/12/14, mt-8/14, p-5/8). Levers = the repeated utility values in the few shared page components + `.card` class in app/globals.css.

## TRUST AMMO (all real, from DB)
- 13,8xx verified listings (render live count)
- 1.34M Google reviews aggregated across listed salons, avg 4.72★
- "Every listing hand-verified / cross-checked against Google, TrueLocal, Yellow Pages" (existing claim, keep)
- Per-salon: real Google rating + review count + "Last visit" date where present

## CONSTRAINTS HONOURED
SEO frozen (URLs/titles/H1s/canonicals/robots/schema structure untouched; additive content OK) · colours frozen (existing tokens only) · Stripe featured dormant · live counts only · confidence_score < 30 stays hidden · no fake urgency.
